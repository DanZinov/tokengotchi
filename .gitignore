# dependencies
node_modules/

# build artifacts (regenerated by `npm run build`; bundled into the npm package on publish)
dist/
bin/
*.tgz
*.zip

# os / editor
.DS_Store
.playwright-mcp/
# loose verification screenshots at repo root (not public/ icons or docs/ assets)
/*.png
/*.jpeg

# internal strategy/architecture doc — kept out of the public repo
TokenForge-PRD*.md

# raw screen-recording source (compressed docs/demo.mp4 is what ships)
/*.mov
